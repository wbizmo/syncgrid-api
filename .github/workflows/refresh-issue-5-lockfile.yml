name: Refresh issue 5 lockfile

on:
  push:
    branches: [fix/5-fastify-security]

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/5-fastify-security
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install --package-lock-only --ignore-scripts
      - name: Commit generated lockfile and remove helper workflow
        run: |
          rm .github/workflows/refresh-issue-5-lockfile.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json .github/workflows/refresh-issue-5-lockfile.yml
          git commit -m "chore: refresh lockfile for Fastify security update"
          git push origin HEAD:fix/5-fastify-security
